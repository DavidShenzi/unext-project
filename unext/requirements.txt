# PyTorch is intentionally NOT pinned here. Install the build that matches your
# CUDA version first (see https://pytorch.org/get-started/locally/); installing
# torch from this file can pull a CPU-only or incompatible wheel over it.
# Developed against torch 2.11.0+cu128.
#
#   pip install -r requirements.txt

timm>=0.9.16
albumentations>=1.4.0
opencv-python>=4.9.0
scikit-learn>=1.4.0
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.66.0
matplotlib>=3.8.0
ptflops>=0.7.3

# only needed to rebuild the slide deck (make_pptx.py)
python-pptx>=0.6.23
